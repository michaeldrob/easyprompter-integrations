{
  "name": "easyprompter-devices",
  "version": "1.0.0",
  "private": true,
  "description": "EasyPrompter device plugin monorepo — Stream Deck, Companion, and shared remote-client",
  "scripts": {
    "build": "pnpm -r build",
    "build:streamdeck": "pnpm --filter streamdeck-plugin build",
    "build:companion": "pnpm --filter companion-module-easyprompter build",
    "typecheck": "pnpm -r typecheck"
  },
  "author": "EasyPrompter",
  "license": "MIT"
}
